name: Pinterest Publisher

on:
  schedule:
    # 10 pins/jour : 2 slots x 5 pins
    # 08h Tunis = 07:00 UTC
    - cron: '0 7 * * *'
    # 17h Tunis = 16:00 UTC
    - cron: '0 16 * * *'
  workflow_dispatch:
    inputs:
      target_date:
        description: 'Date cible (ex: 2026-06-20) - vide = auto'
        required: false
        default: ''
      period:
        description: 'Slot (08h ou 17h) - vide = auto'
        required: false
        default: ''

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Publish pins
        env:
          PINTEREST_ACCESS_TOKEN: ${{ secrets.PINTEREST_ACCESS_TOKEN }}
          TARGET_DATE: ${{ github.event.inputs.target_date }}
          PERIOD: ${{ github.event.inputs.period }}
        run: |
          if [ -n "$TARGET_DATE" ] && [ -n "$PERIOD" ]; then
            python publish_period.py "$TARGET_DATE" "$PERIOD"
          else
            python publish_period.py
          fi

      - name: Commit state files
        run: |
          git config user.name "pinterest-bot"
          git config user.email "bot@noreply.github.com"
          git add published_done.json boards.json publish_log.txt pin_img_idx.json pin_auto_state.json
          git diff --cached --quiet || git commit -m "publish: $(date -u +%Y-%m-%d_%Hh)"
          git pull --rebase origin master || true
          git push
